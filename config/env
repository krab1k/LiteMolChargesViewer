# export CONTEXT_URL=/litemolchargesviewer/	# do not forget / at the start and in the end!
export CONTEXT_URL=/ # for use on root context
export ASSETS_PATH=static/litemol/	# do not add / at the start!
if [ -z "${VERSION}" ]
then
	export VERSION=r6.3	# set version here manualy
else
	export VERSION=${VERSION}	# or export VERSION variable before npm run build
fi


